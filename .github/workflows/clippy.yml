name: clippy

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  clippy:
    name: clippy
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: run clippy
        run: make clippy
